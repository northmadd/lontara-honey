<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Let Apache serve real files and folders, and route all app URLs to React.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
